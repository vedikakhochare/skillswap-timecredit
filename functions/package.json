{
    "name": "time-share-exchange-functions",
    "private": true,
    "engines": {
        "node": "20"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "deploy": "npm run build && firebase deploy --only functions",
        "lint": "echo 'no lint'"
    },
    "dependencies": {
        "firebase-admin": "^12.7.0",
        "firebase-functions": "^5.0.1"
    },
    "devDependencies": {
        "typescript": "^5.8.3"
    }
}